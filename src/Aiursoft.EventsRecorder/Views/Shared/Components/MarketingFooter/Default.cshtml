@model Aiursoft.EventsRecorder.Views.Shared.Components.MarketingFooter.MarketingFooterViewModel
@using Microsoft.AspNetCore.Localization
<footer class="py-5 bg-dark">
    <div class="container">
        <div class="row mb-4">
            <div class="col-12 col-lg-4">
                <div class="mb-3">
                    <img src="@Model.LogoUrl" alt="logo" style="width: 24px; height: 24px;" loading="lazy" />
                </div>
                <p class="text-white-50">
                    @Localizer["EventsRecorder - A self-hosted, open-source event tracking app. Define custom event types, log structured records, and review your history anytime."]
                </p>
                <div class="dropdown mt-3 mb-3">
                    @{
                        var currentCulture = ViewContext.HttpContext.Features
                            .Get<IRequestCultureFeature>()?
                            .RequestCulture.Culture.Name;
                    }
                    <form asp-controller="Culture" asp-action="Set" method="post" class="d-inline" asp-route-returnUrl="@(Context.Request.Path + Context.Request.QueryString)">
                        <select name="culture" onchange="this.form.submit()" class="form-select form-select-sm w-auto">
                            @foreach (var culture in Aiursoft.WebTools.OfficialPlugins.LocalizationPlugin.SupportedCultures)
                            {
                                <option value="@culture.Key"
                                        selected="@(culture.Key == currentCulture ? "selected" : null)">
                                    @culture.Value
                                </option>
                            }
                        </select>
                    </form>
                </div>
            </div>
            <div class="col-12 col-md-4 col-lg-2 mb-3 mb-md-0">
                <h4 class="text-light fs-5">@Localizer["Resources"]</h4>
                <ul class="list-unstyled ps-0 mb-0 mt-3">
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder/-/raw/master/install.sh" class="text-white-50">@Localizer["Install Script"]</a>
                    </li>
                    <li class="mt-2">
                        <a href="https://hub.docker.com/r/aiursoft/eventsrecorder" class="text-white-50">@Localizer["Docker Image"]</a>
                    </li>
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder/-/issues" class="text-white-50">@Localizer["Issues"]</a>
                    </li>
                </ul>
            </div>
            <div class="col-12 col-md-4 col-lg-2 mb-3 mb-md-0">
                <h4 class="text-light fs-5">@Localizer["Project"]</h4>
                <ul class="list-unstyled ps-0 mb-0 mt-3">
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder" class="text-white-50">@Localizer["Source Code"]</a>
                    </li>
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder/-/issues/new" class="text-white-50">@Localizer["Report an Issue"]</a>
                    </li>
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder/-/merge_requests" class="text-white-50">@Localizer["Contributions"]</a>
                    </li>
                </ul>
            </div>
            <div class="col-12 col-md-4 col-lg-2 mb-3 mb-md-0">
                <h4 class="text-light fs-5">@Localizer["Legal"]</h4>
                <ul class="list-unstyled ps-0 mb-0 mt-3">
                    <li class="mt-2">
                        <a href="https://gitlab.aiursoft.com/aiursoft/eventsRecorder/-/blob/master/LICENSE" class="text-white-50">@Localizer["Licence"]</a>
                    </li>
                </ul>
            </div>
            <div class="col-12 col-md-4 col-lg-2 mb-3 mb-md-0">
                <h4 class="text-light fs-5">@Localizer["Social"]</h4>
                <ul class="list-unstyled ps-0 mb-0 mt-3">
                    <li class="mt-2">
                        <i class="fa-brands fa-gitlab"></i>
                        <a href="https://gitlab.aiursoft.com/aiursoft" class="text-white-50" target="_blank">@Localizer["GitLab"]</a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <p class="text-white-50 mt-4 text-center mb-0">
                    © @DateTime.Now.Year <a href="@Model.BrandHomeUrl" target="_blank">@Model.BrandName</a>
                    @if (!string.IsNullOrWhiteSpace(Model.Icp) && System.Globalization.CultureInfo.CurrentCulture.Name == "zh-CN")
                    {
                        <span> | </span>
                        <a href="https://beian.miit.gov.cn/" target="_blank">@Model.Icp</a>
                    }
                </p>
            </div>
        </div>
    </div>
</footer>
